server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/itcity
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: create
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
